{% extends 'base.html.twig' %}

{% block title %}CategoryPublication index{% endblock %}

{% block content %}
<br>
<br>
<br>
<br>
<br>
<br>
    <h1>CategoryPublication index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for category_publication in category_publications %}
            <tr>
                <td>{{ category_publication.id }}</td>
                <td>{{ category_publication.titre }}</td>
                <td>{{ category_publication.description }}</td>
                <td>
                    <a href="{{ path('category_publication_show', {'id': category_publication.id}) }}">show</a>
                    <a href="{{ path('category_publication_edit', {'id': category_publication.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('category_publication_new') }}">Create new</a>
{% endblock %}
