{% extends 'base.html.twig' %}

{% block title %}CategoryPublication{% endblock %}

{% block content %}
    <h1>CategoryPublication</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_publication.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ category_publication.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ category_publication.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_publication_index') }}">back to list</a>

    <a href="{{ path('category_publication_edit', {'id': category_publication.id}) }}">edit</a>

    {{ include('category_publication/_delete_form.html.twig') }}
{% endblock %}
