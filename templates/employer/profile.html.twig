{% extends 'base.html.twig' %}

{% block content %}

    <center> <h2>Hello {{ app.user.name }} </h2></center>
    <br>
    <br>
    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ app.user.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ app.user.name }}</td>
        </tr>
        <tr>
            <th>Mdp</th>
            <td>{{ app.user.mdp }}</td>
        </tr>
        <tr>
            <th>Mail</th>
            <td>{{ app.user.mail }}</td>
        </tr>
        <tr>
            <th>Num</th>
            <td>{{ app.user.num }}</td>
        </tr>
        <tr>
            <th>Localisation</th>
            <td>{{ app.user.localisation }}</td>
        </tr>
        <tr>
            <th>Ctegorie</th>
            <td>{{ app.user.categorie }}</td>
        </tr>
        <tr>
            <th>Image</th>

            <td><img src="{{ asset("assets/img/") }}{{app.user.img}}" alt="" class="img"></td>
        </tr>

        </tbody>
    </table>
    <a class="btn btn-success" href="{{ path('employer_update', {'id': app.user.id}) }}">edit</a>


{% endblock %}